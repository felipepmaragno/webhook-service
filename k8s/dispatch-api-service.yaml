apiVersion: v1
kind: Service
metadata:
  name: dispatch-api
  namespace: dispatch
  labels:
    app.kubernetes.io/name: dispatch-api
    app.kubernetes.io/component: api
spec:
  selector:
    app: dispatch-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
