# Execution Plan: v0.4.0 — Validation pipeline hardening

> Status: **Done** — move to `done/` after this change
> Baseline: branch `feat/validation-pipeline-hardening`
> Goal: strengthen Dispatch's automated guard rails with a layered CI pipeline, Testcontainers-backed infrastructure tests, and thin end-to-end smoke coverage before larger refactors.

## Pre-conditions

- [x] Dedicated feature branch created for this increment
- [x] `go build ./...` passes
- [x] `go test ./...` passes
- [x] `README.md`, `PROGRESS.md`, and testing docs reviewed before edits

---

## Phase 1: Align harness and current baseline

### Step 1: Create and track the execution plan
- [x] Create this active exec plan file in `docs/exec-plans/active/`
- [x] Confirm current baseline behavior and CI/doc mismatches
- [x] Record any environment-specific verification caveats encountered locally
- **Verify:** `git status --short` shows this plan file and no accidental unrelated edits

### Step 2: Define the target validation layers in repo docs
- [x] Update docs to reflect the intended layered pipeline:
      unit/component tests, infra-backed integration tests, thin E2E smoke, non-blocking load/smoke
- [x] Ensure docs match the existing harness language and project conventions
- **Verify:** documentation consistently describes the same validation strategy

---

## Phase 2: Normalize infrastructure-backed tests

### Step 3: Standardize Redis integration tests on Testcontainers
- [x] Replace localhost-dependent Redis test setup with Testcontainers-managed Redis
- [x] Reuse helper patterns similar to Postgres integration tests where practical
- [x] Keep fallback/error-path tests that do not require a real Redis instance
- **Verify:** `go test ./internal/resilience/...` passes without requiring host Redis

### Step 4: Keep repository integration tests in the enforced baseline
- [x] Confirm PostgreSQL integration tests remain green with the current helper pattern
- [x] Ensure CI includes repository integration coverage instead of excluding it
- **Verify:** repository tests are part of the automated validation path

---

## Phase 3: Add end-to-end smoke tests

### Step 5: Add test bootstrap helpers for in-process services
- [x] Add reusable bootstrap code to start API services from Go with explicit config
- [x] Start PostgreSQL, Redis, and Kafka with Testcontainers
- [x] Provision required Kafka topic(s) for the tests
- [x] Use an in-test HTTP receiver to observe deliveries deterministically
- **Verify:** bootstrap can start and stop the system cleanly inside tests

### Step 6: Implement thin E2E scenarios
- [x] Happy path: create subscription → publish event → delivery observed → event marked delivered
- [x] Retry path: temporary receiver failure → retry processing → eventual delivery
- [x] Prefer polling/assertion helpers over arbitrary sleeps
- **Verify:** targeted E2E package passes locally in an environment with Docker/Testcontainers

---

## Phase 4: Harden CI/CD pipeline

### Step 7: Update GitHub Actions to enforce the layered gate
- [x] Keep lint and build jobs
- [x] Add/adjust jobs so PR validation runs:
      fast automated tests, infra-backed integration tests, thin E2E smoke
- [x] Remove reliance on host-local services for Redis-backed tests
- [x] Keep load testing outside the PR-blocking path
- **Verify:** workflow structure matches the documented validation model

---

## Phase 5: Documentation, progress, and closure

### Step 8: Update project docs to match the implemented baseline
- [x] `README.md`: local test/CI expectations and validation layers
- [x] `PROGRESS.md`: verified state, known gaps, next starting point
- [x] `docs/adr/009-testing-strategy.md` and related docs/spec text updated if needed
- [x] `docs/limitations.md` updated where implemented coverage removes a stated gap
- **Verify:** docs no longer describe implemented validation as merely planned

## Closure

- [x] `go build ./...` passes
- [x] `go test ./...` passes
- [x] Focused verification commands for resilience, kafka, race-gated packages, and E2E pass
- [x] Active exec plan fully checked off
- [x] Exec plan moved to `done/`
- [x] `PROGRESS.md` updated to the new verified baseline
