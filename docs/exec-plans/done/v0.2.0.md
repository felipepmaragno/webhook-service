# Execution Plan: v0.2.0 — Stabilize test coverage

> Status: **Done** — moved to done/ after this commit
> Baseline: commit `e6227c38`, coverage 35.6%
> Completed: 2026-06-09, commit `4b9da78`
> Coverage achieved: 51.9% total (was 35.6%)

## Pre-conditions

- [x] `go build ./...` passes
- [x] `go test ./...` passes
- [x] `docs/audit.md` exists and reflects current state

---

## Phase 1: Fix test infrastructure

### Step 1: Fix incomplete schema in the postgres test helper

- [x] Create `internal/repository/postgres/testhelper_test.go` with `setupIntegrationDB`
      that applies real migrations from `migrations/` instead of inline DDL
- [x] Verify that existing `batcher_test.go` still passes with the new helper
- **Verify:** `go test ./internal/repository/postgres/... -v -run TestBatcher` passes ✓

---

## Phase 2: Repository integration tests

### Step 2: Integration tests for EventRepository

- [x] `Create` — happy path, duplicate ID (ON CONFLICT DO NOTHING)
- [x] `CreateBatch` — empty batch, multiple events
- [x] `GetByID` — found, not found (returns `ErrNotFound`)
- [x] `GetPendingEvents` — filters by status, respects `next_attempt_at`, respects `limit`
- [x] `UpdateStatus` — state transitions, verifies fields
- [x] `UpdateStatusBatch` — multiple events
- [x] `RecordAttempt` — records attempt with `status_code`, `error`, `duration_ms`
- [x] `RecordAttemptBatch` — batch of attempts
- [x] `GetAttemptsByEventID` — returns ordered by `attempt_number`
- [x] Add `Shutdown` to `repository.EventRepository` interface (inconsistency fix)
- **Verify:** coverage of `repository/postgres` >70% ✓ (achieved 89.8%)

### Step 3: Integration tests for SubscriptionRepository

- [x] `Create` — happy path, with secret
- [x] `GetByID` — found, not found
- [x] `GetActive` — returns only `active = TRUE`
- [x] `GetByEventType` — exact match, wildcard `order.*`, `*`
- [x] `GetByEventTypes` — multiple types, in-memory grouping logic
- [x] `Delete` — soft-delete sets `active = FALSE`, nonexistent ID returns `ErrNotFound`
- **Verify:** coverage of `repository/postgres` >70% ✓ (achieved 89.8%)

---

## Phase 3: Kafka refactoring + tests

### Step 4: Extract MessageReader and test consumer

- [x] Extract `MessageReader` interface in `internal/kafka/consumer.go`
- [x] Change `reader` field from `*kafka.Reader` to `MessageReader`
- [x] Add `NewConsumerWithReader` for test injection (kept `NewConsumer` for production)
- [x] `go build ./...` passes after refactoring
- [x] Write `consumer_test.go` covering `collectBatch`, `processBatchAndCommit`, `consumeLoop`
- **Verify:** `go test ./internal/kafka/... -v -run TestConsumer` passes ✓

### Step 5: Extract MessageWriter and test producer

- [x] Extract `MessageWriter` interface in `internal/kafka/producer.go`
- [x] Change `writer` field from `*kafka.Writer` to `MessageWriter`
- [x] Add `NewProducerWithWriter` for test injection (kept `NewProducer` for production)
- [x] `go build ./...` passes after refactoring
- [x] Write `producer_test.go` covering `Publish`, `PublishBatch`, trace ID propagation
- [x] Document that `PublishBatch` does NOT propagate trace ID (known gap — see audit.md)
- **Verify:** `go test ./internal/kafka/... -v -run TestProducer` passes ✓

---

## Phase 4: API handler tests

### Step 6: Cover untested handlers and error paths

- [x] `GetEventAttempts`: happy path, empty, multiple attempts
- [x] `GetSubscriptions`: empty list, with items
- [x] `DeleteSubscription`: happy path, ID not found (404)
- [x] `CreateEvent` error paths: invalid body, publisher error
- [x] `CreateSubscription` error paths: missing fields, invalid body, default rate limit
- **Verify:** `go test ./internal/api/... -v` passes ✓

---

## Closure

- [x] `go test ./... -coverprofile=/tmp/cov.out` — total coverage 51.9% (target was >50%) ✓
- [x] `go test -race ./...` passes ✓
- [x] `go build ./...` passes ✓
- [x] `PROGRESS.md` updated with new verified state
- [x] `docs/audit.md` snapshot section updated
