# Execution Plan: v1.0.0 Release Hardening

> **Status:** Completed
> **Estimated effort:** 1-2 focused engineering sessions
> **Depends on:** v0.14.0 minimal operational readiness
> **Release criterion:** Demonstrate and publish the final v1 state without adding product features.

## Goal

Finish Dispatch v1 by auditing the release gate, running the complete validation matrix, reconciling
the documentation set, and recording the final accepted boundaries.

This is not a feature increment. It exists to prove that the existing system satisfies the accepted
v1 promise or to identify a small release blocker that must be fixed before tagging v1.

## Accepted Contract

- No new product features.
- No broad refactors unless a release-gate defect requires them.
- No API contract hardening beyond a release blocker found during the audit.
- No package extraction from `internal/kafka`.
- No destination model redesign.
- No auth, multi-tenancy, UI, batching, ordering, transformations, or managed-service work.
- Release notes must be honest about accepted limitations and deferred work.
- The v1 release gate in `docs/v1-roadmap.md` is the authority for completion.

## Non-goals

- Do not implement queued `api-contract-hardening.md`.
- Do not implement the internal package-boundaries spike.
- Do not run a large capacity campaign beyond existing smoke/baseline evidence.
- Do not add a full backup/restore automation program.
- Do not add cloud-provider-specific deployment docs.

## Phase 1: Release Gate Audit

- [x] Review every unchecked item in `docs/v1-roadmap.md`.
- [x] For each item, classify it as:
  - satisfied by existing evidence;
  - accepted limitation;
  - deferred post-v1 work;
  - release blocker requiring a small fix.
- [x] Update the roadmap checklist only when evidence is linked or the limitation is explicitly
  accepted.
- [x] Do not convert deferred future work into v1 scope.
- **Verify:** the release gate has no ambiguous unchecked item at the end of the increment.

## Phase 2: Complete Validation Matrix

- [x] Run build validation.
- [x] Run full Go test suite.
- [x] Run race-gated unit/component package suite.
- [x] Run lint.
- [x] Run `make validate-basic`.
- [x] Render Compose files.
- [x] Parse Kubernetes YAML.
- [x] Validate relative Markdown links.
- [x] Run `git diff --check`.
- [x] Refresh coverage evidence if useful for release notes; do not chase arbitrary coverage targets.
- **Verify:** `PROGRESS.md` records exact commands and results.

## Phase 3: Documentation Reconciliation

- [x] Reconcile README, product, spec, architecture, limitations, operations guide, v1 roadmap,
  next steps, ADR status notes, package READMEs, and progress.
- [x] Remove duplicated project-description content, especially repeated explanations across README,
  product, spec, and architecture.
- [x] Update `AGENTS.md` when needed so the harness explicitly defines document responsibilities,
  project-description duplication rules, and exec-plan closure rules.
- [x] Remove stale references to superseded behavior where they describe current state rather than
  historical decisions.
- [x] Keep historical ADRs and learnings intact when they are clearly historical.
- [x] Ensure docs agree on:
  - single trusted environment;
  - at-least-once delivery;
  - per-subscription delivery identity;
  - subscription-scoped `max_delivery_rate`;
  - Redis requirement for distributed rate enforcement;
  - no application auth or multi-tenancy in v1;
  - no new planned feature work after v1.
- **Verify:** current-state docs do not contradict the v1 release boundary, and duplicated project
  descriptions are replaced by short summaries plus links to authoritative docs.

## Phase 4: Final V1 Summary

- [x] Add concise release notes or a v1 summary document.
- [x] Summarize:
  - what Dispatch is;
  - how to validate it locally;
  - what it guarantees;
  - what it intentionally does not guarantee;
  - accepted operational and security boundaries;
  - deferred post-v1 ideas.
- [x] Link the summary from README and `PROGRESS.md`.
- **Verify:** a new reviewer can understand the final v1 state without reading every exec plan.

## Phase 5: Defer Remaining Technical Debt Explicitly

- [x] Confirm `api-contract-hardening.md` remains queued and unversioned.
- [x] Confirm `docs/spikes/internal-package-boundaries.md` remains a spike, not active work.
- [x] Confirm destination-resource modeling, richer rate algorithms, auth, multi-tenancy, and
  Kafka outcome topics remain deferred.
- [x] Update `docs/next-steps.md` so post-v1 work is clearly optional future direction, not the next
  required increment.
- **Verify:** no deferred item is presented as necessary for v1 completion.

## Phase 6: Release Closure

- [x] Move this exec plan to `done/`.
- [x] Update `PROGRESS.md` to state v1.0.0 is complete if the release gate passes.
- [x] Update `docs/v1-roadmap.md` to mark v1.0.0 complete only when all release-gate outcomes are
  resolved.
- [x] Leave the working tree clean and push the branch for review.
- **Verify:** the final state has one clear next action: review/tag the v1 release, not continue
  building features.

## Required Validation Matrix

- [x] `GOCACHE=/tmp/dispatch-gocache go build ./...`
- [x] `GOCACHE=/tmp/dispatch-gocache go test ./...`
- [x] Race-gated package suite
- [x] `GOCACHE=/tmp/dispatch-gocache GOLANGCI_LINT_CACHE=/tmp/dispatch-golangci-lint-cache /tmp/dispatch-bin/golangci-lint run ./... --timeout=5m`
- [x] `make validate-basic`
- [x] `docker compose -f docker-compose.yaml config`
- [x] `docker compose -f docker-compose.benchmark.yaml config`
- [x] `docker compose -f docker-compose.kafka.yaml config`
- [x] `yq eval '.' k8s/*.yaml`
- [x] Relative Markdown link validation
- [x] `git diff --check`

## Progress Log

| Date | Step | Note |
|------|------|------|
| 2026-07-01 | Plan drafted | V1.0 scoped to release-gate audit, validation, docs reconciliation, and final v1 summary. |
| 2026-07-01 | Completed | Release gate closed with validation evidence, v1 summary, docs reconciliation, and accepted limitations for pre-v1 upgrades and full backup/restore drills. |
